apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability-promtail
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/grafana/helm-charts.git
      targetRevision: main
      path: charts/promtail
      helm:
        valueFiles:
          - $values/infra/observability/promtail/values.yaml
    - repoURL: https://github.com/smjobboardapp/jobboard-gitops.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: promtail
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
